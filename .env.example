# Jev via OpenRouter (default backend)
OPENROUTER_API_KEY=sk-or-v1-...

# Or a first-party TypeSafe key, with backend="typesafe" in the config
# TYPESAFE_API_KEY=...

# Hosted transcription, for --model lemonfox (best measured; $0.50 per 3 hours of audio)
# LEMONFOX_API_KEY=...
